# 訂單格式轉換系統 修訂日誌

---

## v4 — 2026-04-02

### 錯誤修正
- **蝦皮加密 Excel 自動辨識失敗**：`_confirm_shopee` 原本使用 `openpyxl` 直接開檔，無法讀取加密檔，改為呼叫 `open_xlsx(password=SHOPEE_XLSX_PASSWORD)` 解密後再比對；並新增檔名前綴（`Order.toship.*`）作為備用辨識條件，解決加密檔解密後欄位仍可能讀不到標頭的邊際情況

---

## v3 — 2026-04-02

### 新功能
- **捷捷寶寶粥官網（jjofficial）轉換模組**
  - 輸入：後台匯出 xlsx（Sales 工作表）
  - 輸出：MMDD黑貓.xlsx + MMDD全家.xlsx + MMDD黑貓托運單.csv
  - 以「全家服務編號 / 7-11 店號」自動分拆黑貓宅配 / 全家超商
  - 加購品展開（包數 × 數量，原價 ÷ 包數，附加購折扣欄）
  - 內建 CODE_MAP 轉換非標準品號（G-0-1、N-1、1P-05-150 等）
  - 零補位品號自動去除（2-01 → 2-1）
  - 無品號的促銷贈品行自動略過
  - 運費欄 > 0 時在訂單末尾自動補 F59900001 運費列
  - 使用獨立品號資料：`捷捷寶寶粥官網/品號資料.csv` + `加購品.csv`

- **優迪通路（yodee）轉換模組**
  - 輸入：後台匯出 xlsx（含日期命名工作表，如 2026-01-26）
  - 輸出：MMDD.xlsx（新細明體 12pt，固定欄寬 A~F）
  - 無標頭的歷史資料工作表自動略過
  - 使用獨立品號資料：`優迪通路/品號資料.csv`

- **蝦皮加密 Excel 支援**
  - 新增 `open_xlsx()` 共用函式（`converters/base.py`）
  - 加密 xlsx 自動以密碼解密後處理（偵測與轉換兩個環節皆支援）
  - 安裝 `msoffcrypto-tool` 套件

### 改善
- `services.py`：各通路品號資料路徑集中以 dict 管理，新增通路更易擴充
- `help.html`：說明頁面更新為 6 種來源，補充捷捷官網與優迪通路說明
- `custom.css`：新增 `badge-jjofficial`（藍）與 `badge-yodee`（灰）樣式

---

## v2 — 2026-04-02

### 錯誤修正
- **樂齡網 PDF 解析失敗**：PO 號碼 regex `[P\s][O\s]` 設計錯誤，當 `P` 與 `O` 之間有空格時，`[O\s]` 會吃掉空格導致後續無法比對，所有樂齡網訂單皆回報「無法從 PDF 擷取有效內容」。改為 `P\s*O[\d\s]+` 正確處理字符間距

### 改善
- **Modal 寬度放大**（桌機使用場景優化）：資料夾瀏覽器 480→700px、轉換失敗決策 520→680px
- **路徑顯示改善**：資料夾瀏覽器的當前路徑從截斷改為橫向捲動，預設顯示末尾（最重要的資料夾名稱），不再截掉關鍵部分

---

## v1 — 2026-04-02

### 錯誤修正
- **A1樂齡官網格式偵測不穩定**：原本以第一筆資料的購買品項是否含「捷捷樂齡食品」來判斷，若第一筆為其他商品則偵測失敗，改為以欄位名稱組合（`Orders` 工作表 + `貨號` + `收件人地址` + `購買品項`）作為識別依據，不再依賴資料內容

---

## v4 — 2026-04-01

### 新功能
- **A1樂齡官網（a1leage）轉換模組**：新增第 4 種訂單來源支援
  - 輸入：後台匯出 xlsx（Orders 工作表，含「捷捷樂齡食品」）
  - 輸出：MMDD.xlsx + MMDD黑貓.csv
  - 貨到付款自動加計手續費品項（F59900003，$30）
  - 使用獨立品號資料：`A1樂齡官網/品號資料.csv`
- **說明頁**（/help）：列出 4 種支援格式的輸入規格、識別特徵、操作流程
- **轉換失敗決策 Modal**：有失敗項目時停下來詢問使用者
  - 接受部分結果 / 放棄此次轉換（rollback）/ 新增品號別名
  - 放棄時自動刪除輸出檔、歸檔原始檔、DB 記錄

### 改善
- **資料夾路徑自動記憶**：掃描成功後自動寫入 config.json，下次開啟直接帶入
- **資料夾瀏覽器起點**：從已設定的路徑出發，而非使用者根目錄
- **移除設定頁**，導覽列改為說明頁
- **品號別名新增 UI 改版**：輸入關鍵字即時搜尋，選擇式填寫目標品項
  - 依 source_type 寫入對應的品號資料檔（a1leage 寫 A1樂齡官網/品號資料.csv）
  - a1leage 別名改為複製完整品項資料並換品號，確保 SKU 查找能命中
- **手動指定來源**下拉新增「A1樂齡官網」選項（原本遺漏）

### 錯誤修正
- **A1婦幼展折扣金額 bug**：原本誤將「訂單總金額」填入折扣欄，修正為加總明細檔中負數金額的絕對值
- **A1樂齡官網欄位相容性**：
  - 2月格式無 `商品原價` 欄位 → 全部品項被略過（成功 0 筆）
  - 3月格式組合子品項以 `商品單價=0` 標記（非 `商品原價=0`）
  - 統一規則：有 `商品單價` 欄用它判斷，否則用 `商品原價`
- **start.sh**：重新啟動前先砍掉佔用 port 5099 的舊程序

---

## v3 — 2026-04-01

### 錯誤修正
- **start.bat `/dev/null` 問題**：Windows 不支援 `/dev/null`，改為 `nul`
- **start.bat 找不到 uv**：新增明確的安裝指引（`winget install astral-sh.uv`）

---

## v2 — 2026-03-31

### 新功能
- **資料夾瀏覽器 Modal**：不需複製貼上路徑，點選圖示即可瀏覽選取資料夾（跨平台）
- **檔案勾選修正**：掃描結果可正常勾選/取消，支援部分檔案轉換
- **macOS 啟動腳本**（start.sh）：自動啟動伺服器並開啟瀏覽器
- **Windows 啟動腳本**（start.bat）：錯誤時顯示訊息並暫停，方便截圖回報

### 改善
- A1樂齡官網來源識別加入偵測器
- 新增來源標籤與 Badge 樣式（a1leage）

---

## v1 — 2026-03-30（初版）

- 蝦皮（shopee）、A1婦幼展（a1baby）、樂齡網（leage）3 種格式轉換
- Flask 網頁介面，支援掃描、轉換、下載、轉換記錄
- SSE 即時進度回報
- 品號別名新增功能
- 黑貓托運單 CSV 輸出
